import {api,requireKey,logout} from './gm-key-client.js';
const $=s=>document.querySelector(s);let chars=[],cid=null,glyphMode='root',rootLibrary=[],refreshing=false;const say=t=>{const e=$('#statusMessage');if(e)e.textContent=t};
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const COMBOS={'Ateş|Rüzgâr':'Alev Püskürtme','Buz|Rüzgâr':'Kar Fırtınası','Bitki|Taş':'Kök Duvar','Bitki|Işık':'Şifa Çiçeği','Ateş|Taş':'Erimiş Kaya','Buz|Işık':'Prizma Bariyeri','Rüzgâr|Taş':'Taş Mermiler','Bitki|Buz':'Kristal Dikenler'};
const pair=(a,b)=>[a,b].sort((x,y)=>x.localeCompare(y,'tr')).join('|');const comboName=(a,b)=>COMBOS[pair(a,b)]||'';
function setMode(mode){glyphMode=mode;document.querySelectorAll('[data-glyph-mode]').forEach(b=>b.classList.toggle('active',b.dataset.glyphMode===mode));$('#glyphRootFields').hidden=mode!=='root';$('#glyphCombineFields').hidden=mode!=='combine';$('#glyphCustomFields').hidden=mode!=='custom';if($('#saveGlyph'))$('#saveGlyph').hidden=mode!=='root';if(mode==='combine')updateCombo();}
function updateCombo(){const a=$('#glyphRootA').value,b=$('#glyphRootB').value,n=comboName(a,b),input=$('#glyphCombineName');if(a===b){$('#glyphCombineHint').textContent='İki farklı kök seç.';return}if(!input.dataset.edited)input.value=n;$('#glyphCombineHint').textContent=n?`${a} + ${b} → ${n}`:`${a} + ${b} için kanonik ad yok; adı sen belirleyebilirsin.`;}
function optionHtml(selected=''){return rootLibrary.map(r=>`<option value="${esc(r.name)}"${r.name===selected?' selected':''}>${esc(r.name)}${r.is_core?'':' · Keşfedildi'}</option>`).join('')}
function renderRootLibrary(){const current=$('#glyphName')?.value||'';const a=$('#glyphRootA')?.value||'';const b=$('#glyphRootB')?.value||'';if($('#glyphName'))$('#glyphName').innerHTML=optionHtml(current);if($('#glyphRootA'))$('#glyphRootA').innerHTML=optionHtml(a||rootLibrary[0]?.name||'');if($('#glyphRootB')){$('#glyphRootB').innerHTML=optionHtml(b||rootLibrary[1]?.name||rootLibrary[0]?.name||'');if($('#glyphRootB').value===$('#glyphRootA').value&&rootLibrary[1])$('#glyphRootB').value=rootLibrary[1].name;}if($('#rootLibraryPreview'))$('#rootLibraryPreview').textContent=rootLibrary.length?`Kütüphane: ${rootLibrary.map(r=>r.name).join(' · ')}`:'Kök Glif kütüphanesi boş.';if(glyphMode==='combine')updateCombo();}
async function loadRootLibrary(){const d=await api('gm-key-glyphs',{action:'library'});rootLibrary=d.roots||[];renderRootLibrary();return rootLibrary;}
function resolveGlyph(){if(glyphMode==='root'){const discovered=$('#glyphDiscoveredRoot')?.value.trim()||'';const name=discovered||$('#glyphName').value;const note=$('#glyphNote').value.trim();return {name,description:[discovered?'Keşfedilmiş Kök Glif':'Kök Glif',note].filter(Boolean).join(' · ')||null,rootLibrary:!!discovered,libraryDescription:note||null};}if(glyphMode==='custom'){const n=$('#glyphCustomName').value.trim();return {name:n,description:['Özel GM Glifi',$('#glyphNote').value.trim()].filter(Boolean).join(' · ')||null};}const a=$('#glyphRootA').value,b=$('#glyphRootB').value;if(a===b)return {error:'Kombine Glif için iki farklı kök seç.'};const n=$('#glyphCombineName').value.trim()||comboName(a,b);return {name:n,description:[`Kombine Glif: ${a} + ${b}`,$('#glyphNote').value.trim()].filter(Boolean).join(' · ')||null};}
async function loadCharacters(){const d=await api('gm-key-characters',{action:'list'});chars=d.characters||[];const keep=cid||$('#characterSelect')?.value||'';$('#characterSelect').innerHTML='<option value="">Karakter seç...</option>'+chars.map((c,i)=>`<option value="${c.id}">${i+1}. ${esc(c.character_name)} · ${esc(c.race)}</option>`).join('');if(keep&&chars.some(c=>c.id===keep)){$('#characterSelect').value=keep;cid=keep}}
async function loadDetails(){cid=$('#characterSelect').value||null;if(!cid){$('#glyphStatus').value='Karakter seç';$('#glyphList').innerHTML='<div class="placeholder">Karakter seç.</div>';$('#glyphCount').textContent='0 Glif';return}const d=await api('gm-key-details',{character_id:cid});const g=d.glyphs||[];$('#glyphStatus').value=`${g.length} bilinen Glif`;$('#glyphCount').textContent=`${g.length} Glif`;$('#glyphList').innerHTML=g.length?g.map(x=>`<div class="resource-row compact-row"><div><strong>${esc(x.name)}</strong><small>${esc(x.description||'')}</small></div><span></span><button class="btn danger mini" data-remove="${x.id}">Kaldır</button></div>`).join(''):'<div class="placeholder">Bu karakterde bilinen Glif yok.</div>';$('#glyphList').querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>removeGlyph(b.dataset.remove));}
async function saveRootGlyph(){if(glyphMode!=='root')return say('Kütüphaneye kaydetme Kök Glif sekmesinde kullanılır.');const name=$('#glyphDiscoveredRoot')?.value.trim()||'';if(!name)return say('Kaydetmek için Keşfedilen Yeni Kök Glif alanına bir ad yaz.');const description=$('#glyphNote').value.trim()||null;const d=await api('gm-key-glyphs',{action:'save_root',name,description});await loadRootLibrary();if($('#glyphName')&&rootLibrary.some(r=>r.name.toLocaleLowerCase('tr')===name.toLocaleLowerCase('tr'))){const found=rootLibrary.find(r=>r.name.toLocaleLowerCase('tr')===name.toLocaleLowerCase('tr'));if(found)$('#glyphName').value=found.name;}$('#glyphDiscoveredRoot').value='';say(d.existing?`${name} zaten Kök Glif Kütüphanesi'nde kayıtlı.`:`${name} Kök Glif Kütüphanesi'ne kaydedildi.`);}
async function giveGlyph(){if(!cid)return say('Önce karakter seç.');const g=resolveGlyph();if(g.error)return say(g.error);if(!g.name)return say('Glif adı gerekli.');await api('gm-key-glyphs',{action:'add',character_id:cid,name:g.name,description:g.description,root_library:g.rootLibrary===true,library_description:g.libraryDescription||null});if(g.rootLibrary){await loadRootLibrary();say(`${g.name} oyuncuya verildi ve Kök Glif Kütüphanesi'ne kaydedildi.`);}else say(`${g.name} oyuncuya verildi.`);$('#glyphNote').value='';if(glyphMode==='custom')$('#glyphCustomName').value='';if(glyphMode==='root'&&$('#glyphDiscoveredRoot'))$('#glyphDiscoveredRoot').value='';await loadDetails();}
async function removeGlyph(id){await api('gm-key-glyphs',{action:'remove',glyph_id:id});say('Glif oyuncudan kaldırıldı.');await loadDetails();}
async function refresh(){if(document.hidden||refreshing)return;refreshing=true;try{await loadCharacters();if(cid){$('#characterSelect').value=cid;await loadDetails()}}catch{}finally{refreshing=false}}
(async()=>{if(!requireKey())return;document.querySelectorAll('[data-glyph-mode]').forEach(b=>b.onclick=()=>setMode(b.dataset.glyphMode));$('#glyphRootA').onchange=()=>{$('#glyphCombineName').dataset.edited='';updateCombo()};$('#glyphRootB').onchange=()=>{$('#glyphCombineName').dataset.edited='';updateCombo()};$('#glyphCombineName').oninput=e=>{e.target.dataset.edited=e.target.value.trim()?'1':''};$('#characterSelect').onchange=()=>loadDetails().catch(e=>say('Hata: '+e.message));$('#saveGlyph').onclick=()=>saveRootGlyph().catch(e=>say('Hata: '+e.message));$('#giveGlyph').onclick=()=>giveGlyph().catch(e=>say('Hata: '+e.message));$('#logout').onclick=logout;setMode('root');await Promise.all([loadRootLibrary(),loadCharacters()]);setInterval(refresh,10000);window.addEventListener('focus',refresh);say('Hazır.');})().catch(e=>say('Hata: '+e.message));
