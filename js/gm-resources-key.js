import {api,requireKey,logout} from './gm-key-client.js';
const $=s=>document.querySelector(s);let chars=[],cid=null;const say=t=>{const e=$('#statusMessage');if(e)e.textContent=t};
async function loadCharacters(){const d=await api('gm-key-characters',{action:'list'});chars=d.characters||[];const keep=cid||$('#characterSelect')?.value||'';$('#characterSelect').innerHTML='<option value="">Karakter seç...</option>'+chars.map((c,i)=>`<option value="${c.id}">${i+1}. ${c.character_name} · ${c.race}</option>`).join('');if(keep&&chars.some(c=>c.id===keep)){$('#characterSelect').value=keep;cid=keep}}
async function loadStatus(){cid=$('#characterSelect').value||null;if(!cid){$('#characterStatus').value='Karakter seç';$('#liveFeed').textContent='Karakter seçildiğinde canlı durum burada görünür.';return}const d=await api('gm-key-details',{character_id:cid});const c=d.character,r=d.runtime,items=d.items||[];$('#characterStatus').value=`Focus ${r?.focus_current??0}/${c?.focus_max??0} · ${items.length} eşya`;$('#focusMax').value=c?.focus_max??0;$('#liveFeed').textContent=`${c?.character_name||'Karakter'} · Focus ${r?.focus_current??0}/${c?.focus_max??0} · ${items.length} eşya`;}
async function giveItem(){if(!cid)return say('Önce karakter seç.');const name=$('#itemName').value.trim();const amount=Math.max(1,Number($('#itemAmount').value)||1);const max=Math.max(amount,Number($('#itemMax').value)||amount);if(!name)return say('Eşya adı gerekli.');await api('gm-key-items',{action:'give',character_id:cid,name,amount,max_quantity:max,description:$('#itemDescription').value.trim()||null});say(name+' oyuncuya verildi.');await loadStatus();}
async function focus(){if(!cid)return say('Önce karakter seç.');await api('gm-key-focus',{character_id:cid,focus_max:Math.max(0,Number($('#focusMax').value)||0),delta:Number($('#focusDelta').value)||0});say('Focus güncellendi.');$('#focusDelta').value='0';await loadCharacters();$('#characterSelect').value=cid;await loadStatus();}
(async()=>{if(!requireKey())return;await loadCharacters();$('#characterSelect').onchange=()=>loadStatus().catch(e=>say('Hata: '+e.message));$('#giveItem').onclick=()=>giveItem().catch(e=>say('Hata: '+e.message));$('#updateFocus').onclick=()=>focus().catch(e=>say('Hata: '+e.message));$('#logout').onclick=logout;setInterval(async()=>{try{await loadCharacters();if(cid){$('#characterSelect').value=cid;await loadStatus()}}catch{}},2200);say('Hazır.');})().catch(e=>say('Hata: '+e.message));
